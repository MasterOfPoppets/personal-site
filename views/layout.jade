doctype
html(ng-app='GHApp' ng-controller='GHCtrl')
    head
        title #{title} Gareth Hughes
        link(rel='stylesheet', href='/stylesheets/style.css')
        link(rel='stylesheet', href='//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css')
        script(src='/lib/angular/angular.min.js')
        script(src='/javascripts/app.js')
    body
        header
            h1.pull-left Gareth Hughes
            #social-links.pull-right
                ul
                    li(ng-repeat='socialLink in socialLinks')
                        a(target='_blank', href='{{ socialLink.link }}')
                            i.fa(class='{{ socialLink.icon }}')
            .clearfix
            .separator
            nav.pull-right
                ul
                    li(ng-repeat='siteLink in siteLinks')
                        a(href='{{ siteLink.link }}') {{ siteLink.name }}
            .clearfix
        .container
            .jumbotron
                block jumbotron-content
            .main-content
                block main-content
        footer
            p Running on node.js with Express, Jade and Stylus